#include <stdio.h>
#include <math.h>
#include <string.h>

int main(){
    int t, d, v, s, w, p;
    int C, F, K;
    int cm, mm, km;
    int cg, mg, kg;
    int kl, ml, l;
    int ms, s, min, hrs;

    printf("This calculator converts units of Temperature(t)\:n Celcius(C)\n Farhenheit(F)\n Kelvin(k)\n" );
    printf("");
}

